import 'package:flutter/material.dart';
// create object for category -> fake data -> take data to category_item -> categories_page
class Category{
  int? id;
  String? content;
  Color? color;
  Category({this.id,this.content,this.color});
}